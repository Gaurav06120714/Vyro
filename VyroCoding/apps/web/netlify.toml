[build]
  base    = "VyroCoding/apps/web"
  command = "pnpm install --no-frozen-lockfile && next build"
  publish = ".next"

[build.environment]
  NEXT_TELEMETRY_DISABLED = "1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
